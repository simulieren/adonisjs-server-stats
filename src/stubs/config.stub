{{{
  exports({ to: app.configPath('server_stats.ts') })
}}}
import { defineConfig } from 'adonisjs-server-stats'

export default defineConfig({
  // Auto-detects installed packages and enables matching collectors.
  // Override with a specific array if needed.
  // collectors: 'auto',

  // Who can see the stats bar, debug toolbar, and dashboard.
  // authorize: (ctx) => ctx.auth?.user?.role === 'admin',

  // Enable the debug toolbar (SQL queries, events, emails, logs)
  // toolbar: true,

  // Enable the full-page dashboard at /__stats
  // dashboard: true,

  // History window and disk budget for persisted stats. When the database
  // outgrows the size cap the oldest records are pruned regardless of age.
  // dashboard: { retentionDays: 7, maxDbSizeMb: 500 },

  // Log detailed initialization steps (SQLite, migrations, routes, etc.)
  // verbose: true,

  // Nothing is registered in production unless you opt in here. Requires an
  // `authorize` guard, and data capture stays off until you name a subsystem.
  // production: {
  //   enabled: true,
  //   capture: { queries: true },
  //   retentionDays: 3,
  // },
})
